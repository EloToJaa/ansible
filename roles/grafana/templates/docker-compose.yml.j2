version: '3'
services:
  grafana:
    image: grafana/grafana-oss:latest
    container_name: grafana
    volumes:
      - grafana:/var/lib/grafana
    networks:
      - proxy

networks:
  proxy:
    external: true

volumes:
  grafana:
    external: true