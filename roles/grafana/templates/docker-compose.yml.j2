version: '3'
services:
  grafana:
    image: grafana/grafana-oss:latest
    container_name: grafana
    volumes:
      - ${VOLUMES_DIR}/grafana:/var/lib/grafana
    networks:
      - proxy

networks:
  proxy:
    external: true