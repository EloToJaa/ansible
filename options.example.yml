# General options
enable_ssh_keygen: false
enable_username_creation: false
notifications: false
timezone: Europe/Warsaw

# Server Docker section
docker_dir: /mnt/Server/Docker
data_dir: /mnt/Data
run_containers: true

# Samba shares
shares:
  - uri: "//IP/Share"
    mountpoint: "/mnt/Share"

additional_software:
  - 'ssh'